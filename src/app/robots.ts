import { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: "/",
    },
    sitemap: "https://yuggu-tools.com/sitemap.xml", // Replace with actual domain when deployed
  };
}
